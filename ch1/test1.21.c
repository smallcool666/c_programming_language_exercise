#include <stdio.h>
#include <Windows.h>
/**
 *  练习1-21 编写程序entab，将空格串替换为最少数量的制表符和空格，但要保持单词之间的间隔不变。假设制表符终止位的位置
 * 与练习1-20的detab程序的情况相同。当使用一个制表符或者一个空格都可以到达下一个制表符终止位时，选用哪一种替换字符
 * 比较好？
 */
#define MAXLINE 1000	/* maximum input line length */

int main()
{
    system("pause");
    return 0;
}