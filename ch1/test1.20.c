#include <stdio.h>
#include <Windows.h>
/**
 *  练习1-20 编写程序detab，将输入中的制表符替换成适当数目的空格，使空格充满到下一个制表符终止位的地方。假设制表符终止
 * 位的位置是固定的，比如每隔n列就会出现一 个制表符终止位。n应该作为变量还是符号常量呢？
 */
#define MAXLINE 1000	/* maximum input line length */

int main()
{

    system("pause");
    return 0;
}